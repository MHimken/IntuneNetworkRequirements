See [AGENTS.md](AGENTS.md) at the repository root for full agent guidance.

It covers the two codebases (vendored upstream `Get-IntuneNetworkRequirements.ps1`
versus the owned wrapper), the fragile wrapper-to-scanner coupling, owned-code
conventions, the verify commands, and the ASCII-only writing rule.
